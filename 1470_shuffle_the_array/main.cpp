#include <bits/stdc++.h>

using namespace std;
class Solution {
public:
    vector<int> shuffle(vector<int>& nums, int n) {
        vector<int> ans;
        for(int i = 0; i < n; i++){
            ans[i]
        }
        return ans;
    }
};
int main()
{
    vector<int> etwa{1,4,5,6,7,12,32,30};
    Solution sol;
    vector<int> ans = sol.shuffle(etwa,4);
    for(auto x : ans) cout << x << endl;
}
